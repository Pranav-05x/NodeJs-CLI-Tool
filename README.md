# Node.js CLI Line Counter

A command line tool built with Node.js that counts 
the number of lines in a given file.

## Tech Used
- Node.js
- Commander.js

## How to Run

Install dependencies:
npm install

Run the command:
node index.js count <filename>

Example:
node index.js count a.txt
